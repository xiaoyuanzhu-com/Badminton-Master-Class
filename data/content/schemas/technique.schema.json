{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "$id": "technique.schema.json",
  "title": "Technique",
  "description": "A badminton technique category. Lives at _technique.json in each technique folder.",
  "type": "object",
  "required": ["name", "icon"],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Display name of the technique category"
    },
    "icon": {
      "type": "string",
      "minLength": 1,
      "description": "Emoji icon for the technique category"
    },
    "sort_order": {
      "type": "integer",
      "minimum": 0,
      "description": "Explicit display order for the category (lower numbers appear first). Falls back to directory enumeration order if omitted."
    }
  }
}
